SPEC javax.crypto.CipherInputStream
OBJECTS
    byte[] b;
    int off;
    int len;
    java.io.InputStream is;
    javax.crypto.Cipher ciph;

EVENTS
    c1: CipherInputStream(is, ciph);
    
    r1: read();
    r2: read(b);
    r3: read(b, off, len);
    Reads := r1 | r2 | r3;

    c: close();

ORDER
    c1, Reads+, c

CONSTRAINTS
    len > off;

REQUIRES
    initializedCipher[ciph];

ENSURES
    cipheredInputStream[is, ciph];
